var Contextify = require('contextify');
var sandbox = { console : console, prop1 : 'mani'};
Contextify(sandbox);
sandbox.run('console.log(prop1);');
sandbox.save('test');
sandbox.dispose(); // free the resources allocated for the context.
